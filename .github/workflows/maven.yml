name: Build and deploy 

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java version
        uses: actions/setup-java@v1
        with:
          java-version: '17'

      - name: Build with Maven
        run: mvn clean install

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: java-app
          path: '${{ github.workspace }}/target/*.jar'

  deploy-dev:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Development'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: java-app

      - name: Deploy to Azure Web App - Dev
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'projet-note-cicd-dev-back'
          slot-name: 'dev'
          publish-profile: ${{ secrets.DEV_AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: '*.jar'

  deploy-qualif:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Qualification'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: java-app

      - name: Deploy to Azure Web App - Qualif
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'projet-note-cicd-qualid-back'
          slot-name: 'qualif'
          publish-profile: ${{ secrets.QUALIF_AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: '*.jar'

  deploy-prod:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: java-app

      - name: Deploy to Azure Web App - Prod
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'projet-note-cicd-prod-back'
          slot-name: 'production'
          publish-profile: ${{ secrets.PROD_AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: '*.jar'
